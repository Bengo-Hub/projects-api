// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/projects-service/internal/ent/project"
	"github.com/bengobox/projects-service/internal/ent/timelog"
	"github.com/google/uuid"
)

// TimeLog is the model entity for the TimeLog schema.
type TimeLog struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID uuid.UUID `json:"project_id,omitempty"`
	// TaskID holds the value of the "task_id" field.
	TaskID uuid.UUID `json:"task_id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Hours holds the value of the "hours" field.
	Hours float64 `json:"hours,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// LoggedDate holds the value of the "logged_date" field.
	LoggedDate time.Time `json:"logged_date,omitempty"`
	// IsBillable holds the value of the "is_billable" field.
	IsBillable bool `json:"is_billable,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TimeLogQuery when eager-loading is set.
	Edges        TimeLogEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TimeLogEdges holds the relations/edges for other nodes in the graph.
type TimeLogEdges struct {
	// Project holds the value of the project edge.
	Project *Project `json:"project,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProjectOrErr returns the Project value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TimeLogEdges) ProjectOrErr() (*Project, error) {
	if e.Project != nil {
		return e.Project, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: project.Label}
	}
	return nil, &NotLoadedError{edge: "project"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TimeLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case timelog.FieldIsBillable:
			values[i] = new(sql.NullBool)
		case timelog.FieldHours:
			values[i] = new(sql.NullFloat64)
		case timelog.FieldDescription:
			values[i] = new(sql.NullString)
		case timelog.FieldLoggedDate, timelog.FieldCreatedAt, timelog.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case timelog.FieldID, timelog.FieldProjectID, timelog.FieldTaskID, timelog.FieldTenantID, timelog.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TimeLog fields.
func (tl *TimeLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case timelog.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tl.ID = *value
			}
		case timelog.FieldProjectID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value != nil {
				tl.ProjectID = *value
			}
		case timelog.FieldTaskID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field task_id", values[i])
			} else if value != nil {
				tl.TaskID = *value
			}
		case timelog.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				tl.TenantID = *value
			}
		case timelog.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				tl.UserID = *value
			}
		case timelog.FieldHours:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field hours", values[i])
			} else if value.Valid {
				tl.Hours = value.Float64
			}
		case timelog.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				tl.Description = value.String
			}
		case timelog.FieldLoggedDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field logged_date", values[i])
			} else if value.Valid {
				tl.LoggedDate = value.Time
			}
		case timelog.FieldIsBillable:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_billable", values[i])
			} else if value.Valid {
				tl.IsBillable = value.Bool
			}
		case timelog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tl.CreatedAt = value.Time
			}
		case timelog.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tl.UpdatedAt = value.Time
			}
		default:
			tl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TimeLog.
// This includes values selected through modifiers, order, etc.
func (tl *TimeLog) Value(name string) (ent.Value, error) {
	return tl.selectValues.Get(name)
}

// QueryProject queries the "project" edge of the TimeLog entity.
func (tl *TimeLog) QueryProject() *ProjectQuery {
	return NewTimeLogClient(tl.config).QueryProject(tl)
}

// Update returns a builder for updating this TimeLog.
// Note that you need to call TimeLog.Unwrap() before calling this method if this TimeLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (tl *TimeLog) Update() *TimeLogUpdateOne {
	return NewTimeLogClient(tl.config).UpdateOne(tl)
}

// Unwrap unwraps the TimeLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tl *TimeLog) Unwrap() *TimeLog {
	_tx, ok := tl.config.driver.(*txDriver)
	if !ok {
		panic("ent: TimeLog is not a transactional entity")
	}
	tl.config.driver = _tx.drv
	return tl
}

// String implements the fmt.Stringer.
func (tl *TimeLog) String() string {
	var builder strings.Builder
	builder.WriteString("TimeLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tl.ID))
	builder.WriteString("project_id=")
	builder.WriteString(fmt.Sprintf("%v", tl.ProjectID))
	builder.WriteString(", ")
	builder.WriteString("task_id=")
	builder.WriteString(fmt.Sprintf("%v", tl.TaskID))
	builder.WriteString(", ")
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", tl.TenantID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", tl.UserID))
	builder.WriteString(", ")
	builder.WriteString("hours=")
	builder.WriteString(fmt.Sprintf("%v", tl.Hours))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(tl.Description)
	builder.WriteString(", ")
	builder.WriteString("logged_date=")
	builder.WriteString(tl.LoggedDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("is_billable=")
	builder.WriteString(fmt.Sprintf("%v", tl.IsBillable))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(tl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(tl.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TimeLogs is a parsable slice of TimeLog.
type TimeLogs []*TimeLog
